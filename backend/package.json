{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "standard && tap test/*.test.js test/*/*.test.js test/*/*/*.test.js",
    "start": "fastify start app.js",
    "dev": "fastify start -l info -P app.js",
    "lint": "standard --fix"
  },
  "author": "Rafael Mendes Sienna",
  "license": "MIT",
  "dependencies": {
    "fastify": "^1.5.0",
    "fastify-autoload": "^0.4.0",
    "fastify-cli": "^0.16.1"
  },
  "devDependencies": {
    "standard": "^11.0.1",
    "tap": "^11.1.3"
  }
}
